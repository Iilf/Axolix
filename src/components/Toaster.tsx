"use client"

/**
 * src/components/Toaster.tsx
 *
 * Toast notification queue.
 * Spring enter (350ms), ease-in exit (200ms). Auto-dismiss + manual dismiss.
 *
 * Stub — full implementation in the components phase.
 * Rendered in the root layout so toasts are available on every page.
 */

export function Toaster() {
  // Full implementation: toast queue, spring animations, stacking.
  // Stub renders nothing — no layout impact.
  return null
}
